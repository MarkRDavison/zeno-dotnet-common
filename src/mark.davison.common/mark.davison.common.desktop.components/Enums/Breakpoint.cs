namespace mark.davison.common.client.desktop.Enums;

public enum Breakpoint
{
    XS, // < 600px
    S, // 600px > < 960px
    MD, // 960px > < 1280px
    LG, // 1280px > < 1920px
    XL, // 1920px > < 2560px
    XXL // > 2560px
}
